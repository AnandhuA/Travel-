import 'package:flutter/material.dart';

class ComplectedTrip extends StatelessWidget {
  const ComplectedTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Complected"),);
  }
}